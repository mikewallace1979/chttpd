{application, chttpd, [
    {description, "HTTP interface for CouchDB cluster"},
    {vsn, git},
    {registered, [chttpd_sup, chttpd]},
    {applications, [kernel, stdlib, couch, couch_mrview, fabric]},
    {mod, {chttpd_app,[]}}
]}.